// for mobile device layout design

if ($(window).width() < 768) {
    
    //$("form").removeClass('form-control').addClass('form-control form-control-sm');

    var inputGroups = $('.input-group');
    console.log(inputGroups);

}
